extends layout.pug


block styles
    link(rel='stylesheet', href='/webLearn/css/page.css')
block content
    a(href='/webLearn/') Главная
    block lessonVars
    block lesson

    div.floating-box
        if back
            a(href=back) Назад
        div=type
        if next
            a(href=next) Вперед
        else
            a(href='/') Выбор курса
